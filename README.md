# UIKit CLI

**AI 时代的 UI 规范管理系统** - 让 AI 生成的界面始终保持一致的设计语言

[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)
[![Build: Bun](https://img.shields.io/badge/Build-Bun-purple.svg)](https://bun.sh)

## 核心特性

- **一次选择，持续生效** - 选择设计规范后，所有后续开发自动应用
- **5 套专业规范** - 现代简约、暗黑优雅、创意趣味、专业商务、活力科技
- **框架无关** - 支持 React、Vue、HTML 等任何技术栈
- **智能审查** - 自动检查代码是否符合规范标准
- **云端同步** - 规范和命令定义自动从 GitHub 获取最新版本

---

## 快速开始

### 一键安装

```bash
curl -fsSL https://raw.githubusercontent.com/lgh-dev/uikit/main/install.sh | bash
```

安装脚本会：
- 自动检测操作系统（macOS / Linux / Windows）
- 下载对应平台的二进制文件
- 安装到 `/usr/local/bin/uikit`
- 检查版本并自动更新

### 手动安装

1. 从 [Releases](https://github.com/lgh-dev/uikit/releases) 下载对应平台的二进制文件：
   - `uikit-macos-arm64` - macOS Apple Silicon
   - `uikit-macos-x64` - macOS Intel
   - `uikit-linux-x64` - Linux x64
   - `uikit-windows-x64.exe` - Windows x64

2. 安装到系统：
   ```bash
   # macOS / Linux
   sudo mv uikit-macos-arm64 /usr/local/bin/uikit
   sudo chmod +x /usr/local/bin/uikit

   # Windows
   # 将 uikit-windows-x64.exe 重命名为 uikit.exe
   # 移动到 PATH 中的目录（如 C:\Windows\System32）
   ```

3. 验证安装：
   ```bash
   uikit -v
   ```

### 初始化项目

```bash
# 进入你的项目目录
cd /path/to/your/project

# 初始化到 Claude Code
uikit init claude

# 或初始化到 Qoder
uikit init qoder

# 重启对应的 AI 工具
```

初始化后会在项目中创建：
- `.uikit/specs/` - 5个设计规范文件（自动从 GitHub 下载）
- `.uikit/current-spec.json` - 当前选中的规范配置
- `.claude/commands/` 或 `.qoder/commands/` - 3个命令文件

### 开始使用

重启 AI 工具后，输入 `/` 即可看到可用命令：

```bash
# 1. 选择设计规范
/uikit-switch dark-elegant

# 2. 按规范开发功能
/uikit-do 创建登录页面

# 3. 审查代码合规性
/uikit-check
```

**可用规范**: `modern-minimal` | `dark-elegant` | `creative-playful` | `professional-business` | `vibrant-tech`

---

## 设计规范

### Modern Minimal (现代简约)
简洁优雅，中性色调 + 品牌色点缀。适合 SaaS 产品、工具应用。

### Dark Elegant (暗黑优雅)
深色模式，高对比度，精致细节。适合代码编辑器、设计工具。

### Creative Playful (创意趣味)
圆润温暖，多彩柔和。适合教育产品、创意社区。

### Professional Business (专业商务)
稳重可信，深色主调 + 金色蓝色。适合企业官网、金融产品。

### Vibrant Tech (活力科技)
渐变玻璃态，鲜艳多彩。适合创新产品、科技应用。

---

## 命令详解

### /uikit-switch - 切换设计规范

快速切换当前项目使用的设计规范。

**使用方法**：
```bash
# 直接指定规范名
/uikit-switch modern-minimal

# 不带参数，显示所有可用规范供选择
/uikit-switch
```

切换后的规范会保存在 `.uikit/current-spec.json`，后续所有开发都会使用新规范。

### /uikit-do - 按规范开发

使用当前选定的设计规范开发功能。AI 会严格遵循规范中定义的：
- 色彩系统（主色、辅助色、状态色）
- 间距系统（基础单位、间距规模）
- 字体排版（字体家族、字号、字重）
- 圆角、阴影、动画效果
- 组件样式和交互状态

**使用方法**：
```bash
# 描述要开发的功能
/uikit-do 创建一个用户注册表单，包含邮箱、密码、确认密码字段

# AI 会生成符合当前规范的完整代码
```

**注意**：首次使用前，请先用 `/uikit-switch` 选择一个设计规范。

### /uikit-check - 审查合规性

审查已开发的功能是否符合当前选定的设计规范。

**审查项目**：
1. **色彩系统** - 是否使用规范定义的色彩，对比度是否达标
2. **间距系统** - 元素间距、内外边距是否一致
3. **字体排版** - 字体大小、行高、字重是否正确
4. **组件样式** - 组件样式和交互状态是否完整
5. **响应式设计** - 移动端适配、断点设置是否合理

**使用方法**：
```bash
# 提供代码文件路径或直接粘贴代码
/uikit-check

# AI 会给出详细的审查报告和改进建议
```

---

## 组件支持

每个规范都包含完整的组件系统定义，AI 会自动生成符合规范的代码：

- **基础组件**: Button, Input, Textarea, Select, Checkbox, Radio, Switch, Slider, Card, Badge, Avatar
- **反馈组件**: Alert, Toast, Modal, Drawer, Tooltip, Progress
- **导航组件**: Tabs, Breadcrumb, Pagination, Menu, Navbar
- **数据展示**: Table, List, Tag, Timeline, Collapse
- **布局组件**: Container, Grid, Flex, Spacer

---

## 核心优势

### 框架无关
设计规范使用纯设计语言描述，AI 自动识别项目技术栈生成对应代码。支持 React、Vue、Angular、Svelte、Solid、纯 HTML。

### 设计优先
规范描述设计意图而非具体实现，AI 会根据上下文选择最佳实现方式，并自动包含所有交互状态、响应式设计和可访问性支持。

### 一致性保证
统一的设计系统确保色彩、间距、圆角、阴影、动画等所有细节保持一致，无论是不同页面、不同开发者还是不同 AI 工具。

### 云端同步
规范和命令定义存储在 GitHub，每次初始化都获取最新版本。你也可以 fork 仓库创建自己的规范。

---

## 技术支持

### AI 工具
- ✅ Claude Code - 完全支持
- ✅ Qoder - 完全支持
- 🚧 Cursor - 开发中
- 🚧 Windsurf - 开发中

### 前端框架
支持所有主流框架：React、Vue、Angular、Svelte、Solid、原生 HTML

### 样式方案
支持所有样式方案：Tailwind CSS、CSS Modules、styled-components、Emotion、SCSS、原生 CSS

---

## 常见问题

### 如何更新规范到最新版本？

重新运行初始化命令即可：
```bash
uikit init claude
```

规范文件会从 GitHub 自动下载最新版本。

### 如何创建自定义规范？

1. Fork [UIKit 仓库](https://github.com/lgh-dev/uikit)
2. 在 `specs/` 目录添加你的规范文件（参考现有规范格式）
3. 修改 `bin/uikit.js` 中的 `GITHUB_REPO` 指向你的仓库
4. 重新编译二进制文件

### 网络不可用时如何使用？

如果无法访问 GitHub：
1. 手动下载 [specs/](https://github.com/lgh-dev/uikit/tree/main/specs) 和 [commands/](https://github.com/lgh-dev/uikit/tree/main/commands) 目录
2. 将它们放在 UIKit 二进制文件同一目录
3. 运行 `uikit init` 会自动使用本地文件

### 如何查看当前状态？

```bash
uikit status
```

显示：
- 当前选中的规范
- 已安装的命令
- 规范文件列表

---

## 项目结构

```
uikit/
├── bin/                         # CLI 源代码
│   └── uikit.js                 # 主程序（支持从 GitHub 下载）
│
├── specs/                       # 设计规范源文件
│   ├── modern-minimal.md
│   ├── dark-elegant.md
│   ├── creative-playful.md
│   ├── professional-business.md
│   └── vibrant-tech.md
│
├── commands/                    # 命令定义源文件
│   ├── uikit-switch.md
│   ├── uikit-do.md
│   └── uikit-check.md
│
├── dist/                        # 构建输出（不提交到 Git）
│   ├── uikit-macos-x64
│   ├── uikit-macos-arm64
│   ├── uikit-linux-x64
│   └── uikit-windows-x64.exe
│
├── build.js                     # 构建脚本
├── install.sh                   # 安装脚本
├── package.json
├── LICENSE
└── README.md
```

**用户项目中的文件**（运行 `uikit init` 后生成）：
```
your-project/
├── .uikit/                      # UIKit 配置（不提交到 Git）
│   ├── specs/                   # 从 GitHub 下载的规范文件
│   └── current-spec.json        # 当前选中的规范
│
└── .claude/                     # 或 .qoder/（不提交到 Git）
    └── commands/                # 从 GitHub 下载的命令文件
        ├── uikit-switch.md
        ├── uikit-do.md
        └── uikit-check.md
```

---

## 贡献

欢迎贡献新的设计规范、改进建议或问题反馈：

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/amazing-spec`)
3. 提交更改 (`git commit -m 'Add amazing spec'`)
4. 推送到分支 (`git push origin feature/amazing-spec`)
5. 创建 Pull Request

---

## License

MIT License - 详见 [LICENSE](LICENSE) 文件

---

**开始使用 UIKit，让 AI 生成始终保持一致的高质量界面！**
